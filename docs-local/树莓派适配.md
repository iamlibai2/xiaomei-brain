# 树莓派适配（待实现）

## PyArrow/LanceDB AVX 兼容性问题
- 部分 CPU（树莓派、老旧 x86、ARM 云服务器）无 AVX 指令集 → PyArrow SIGILL 崩溃
- **Hermes 方案**：子进程 probe PyArrow 检测，崩了就 fallback 到 `sqlite-bruteforce`（纯 Python）
- **参考**：`nousresearch/hermes-agent` 的 `hermes_state.py`
- **优先级**：低，WSL2 环境无此问题。发版到社区前加。
