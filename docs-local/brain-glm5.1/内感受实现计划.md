# 内感受（Interoception）实现计划

> **For agentic workers:** Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建内感受系统——让 Agent 感知身体运行状态，分层响应（自愈→表达→SOS告警），最终让 LLM 在对话中自然流露生命力。

**Architecture:** 新建 `consciousness/interoception.py` 集中采集+阈值判断+产出 signals，各组件读信号执行行为。不改动现有线程模型。

**Tech Stack:** Python 3.x dataclass, threading (existing Layer0/Living integration)

---

## 文件结构

```
新建:  consciousness/interoception.py   # 集中：采集+判断+信号产出
修改:  consciousness/self_modules.py    # SelfBody +7 内感受字段
修改:  consciousness/layer0.py          # 调用 interoception.tick()
修改:  consciousness/self_image_proxy.py # _render_body() 注入内感受
修改:  base/llm.py                      # LLMClient +退避+切换
修改:  consciousness/living.py          # Living 基类 +限流+SOS
修改:  consciousness/conscious_living.py # _heartbeat() +SOS通道
修改:  drive/engine.py                  # +on_system_stress()/on_system_healthy()
```

---

### Task 1: 创建 Interoception 类

**Files:**
- Create: `src/xiaomei_brain/consciousness/interoception.py`

- [ ] **Step 1: 创建 Interoception 类和 InteroceptionSignals 数据类**

```python
"""Interoception: 内感受——采集身体指标、阈值判断、产出信号。

集中式设计：所有数据采集和阈值判断集中在此，各组件只读信号执行。
不做执行——限流/退避/SOS 由各组件按各自职责完成。
"""

from __future__ import annotations

import logging
import threading
import time
from dataclasses import dataclass, field
from typing import TYPE_CHECKING, Any

if TYPE_CHECKING:
    from .self_modules import SelfBody

logger = logging.getLogger(__name__)

# 最大队列容量（用于计算 queue_pressure）
DEFAULT_MAX_QUEUE_SIZE = 100

# SOS 冷却：同一类型 5 分钟内只发一次
SOS_COOLDOWN_SECONDS = 300

# SOS 追加提醒：30 分钟后未恢复
SOS_REMINDER_SECONDS = 1800

# 自愈阈值
THROTTLE_QUEUE_PRESSURE = 0.8
BACKOFF_CONSECUTIVE_FAILURES = 3
PROVIDER_SWITCH_CONSECUTIVE_FAILURES = 5
SOS_LLM_CASCADE_THRESHOLD = 10


@dataclass
class InteroceptionSignals:
    """产出信号——各组件读取并执行。"""

    # 自愈信号
    throttle: bool = False               # 限流
    backoff_seconds: float = 0.0         # LLM 退避延迟（秒），0 = 不触发
    provider_switch: bool = False        # 切换 LLM provider

    # 告警信号
    sos: bool = False                    # SOS 紧急告警
    sos_reason: str = ""                 # SOS 原因
    sos_message: str = ""                # SOS 文案

    # 压力等级（给 Drive）
    stress_level: str = "none"           # none / mild / moderate / severe

    # 身体数据快照（给 SelfBody 写入）
    thread_health: dict[str, bool] = field(default_factory=dict)
    queue_pressure: float = 0.0
    llm_latency_ms: float = 0.0
    llm_error_rate: float = 0.0
    llm_consecutive_failures: int = 0
    token_usage: float = 0.0
    memory_fullness: str = "清爽"
    burning_duration: float = 0.0

    def as_body_dict(self) -> dict[str, Any]:
        """转为 SelfBody 可直接写入的数据。"""
        return {
            "thread_health": self.thread_health,
            "queue_pressure": self.queue_pressure,
            "llm_latency_ms": self.llm_latency_ms,
            "llm_error_rate": self.llm_error_rate,
            "token_usage": self.token_usage,
            "memory_fullness": self.memory_fullness,
            "burning_duration": self.burning_duration,
        }


class Interoception:
    """内感受：集中采集+判断+设信号。不执行。"""

    def __init__(
        self,
        max_queue_size: int = DEFAULT_MAX_QUEUE_SIZE,
        burn_start_time: float | None = None,
    ) -> None:
        self._max_queue_size = max_queue_size
        self._burn_start_time = burn_start_time or time.time()

        # LLM 错误追踪（窗口统计）
        self._llm_error_window: list[tuple[float, bool]] = []  # [(timestamp, is_error)]
        self._llm_error_window_seconds = 300  # 5 分钟窗口

        # SOS 冷却追踪
        self._sos_last_sent: dict[str, float] = {}     # reason → last sent time
        self._sos_reminder_sent: dict[str, bool] = {}  # reason → whether 30min reminder sent
        self._sos_recovered: set[str] = set()           # already-recovered reasons

        # LLM 回调引用（由外部设置）
        self._llm_callback: Any = None

        # 线程引用（由 Layer0 设置，用于健康检查）
        self._threads: dict[str, threading.Thread] = {}

    # ── 外部接口 ──────────────────────────────────────────

    def set_llm_callback(self, callback: Any) -> None:
        """设置 LLM 客户端引用，用于读取延迟/错误数据。"""
        self._llm_callback = callback

    def set_threads(self, threads: dict[str, threading.Thread]) -> None:
        """设置需监控的线程引用。"""
        self._threads = threads

    def set_burn_start_time(self, t: float) -> None:
        self._burn_start_time = t

    def record_llm_call(self, latency_ms: float, is_error: bool) -> None:
        """记录一次 LLM 调用结果（供 LLMClient 调用）。"""
        self._llm_error_window.append((time.time(), is_error))
        # 清理过期窗口
        cutoff = time.time() - self._llm_error_window_seconds
        self._llm_error_window = [(ts, err) for ts, err in self._llm_error_window if ts > cutoff]

    def tick(self, queue_depth: int) -> InteroceptionSignals:
        """执行一次采集+判断，返回信号。

        Args:
            queue_depth: 当前 Living 消息队列深度
        """
        signals = InteroceptionSignals()

        # ── 1. 采集 ──
        self._collect_thread_health(signals)
        self._collect_queue(signals, queue_depth)
        self._collect_llm(signals)
        self._collect_burning(signals)
        self._collect_memory(signals)

        # ── 2. 自愈判断 ──
        self._evaluate_self_heal(signals)

        # ── 3. SOS 判断 ──
        self._evaluate_sos(signals)

        # ── 4. 压力等级 ──
        self._evaluate_stress(signals)

        return signals

    # ── 采集 ──────────────────────────────────────────────

    def _collect_thread_health(self, signals: InteroceptionSignals) -> None:
        health: dict[str, bool] = {}
        for name, thread in self._threads.items():
            health[name] = thread.is_alive()
        signals.thread_health = health

    def _collect_queue(self, signals: InteroceptionSignals, depth: int) -> None:
        signals.queue_pressure = min(1.0, depth / self._max_queue_size) if self._max_queue_size > 0 else 0.0

    def _collect_llm(self, signals: InteroceptionSignals) -> None:
        if not self._llm_error_window:
            signals.llm_latency_ms = 0.0
            signals.llm_error_rate = 0.0
            signals.llm_consecutive_failures = 0
            return

        total = len(self._llm_error_window)
        errors = sum(1 for _, is_err in self._llm_error_window if is_err)
        signals.llm_error_rate = errors / total if total > 0 else 0.0

        # 连续失败计数（从最近往前数）
        consecutive = 0
        for _, is_err in reversed(self._llm_error_window):
            if is_err:
                consecutive += 1
            else:
                break
        signals.llm_consecutive_failures = consecutive

        # 延迟使用最后一个窗口记录的近似值
        # 实际延迟由 LLMClient.record_llm_call() 传入

    def _collect_burning(self, signals: InteroceptionSignals) -> None:
        signals.burning_duration = (time.time() - self._burn_start_time) / 3600.0

    def _collect_memory(self, signals: InteroceptionSignals) -> None:
        # memory_fullness 需要 LongTermMemory 引用，在实际部署时注入
        # 当前版本使用 token_usage 和队列压力作为代理判断
        if signals.token_usage > 0.9 or signals.queue_pressure > 0.85:
            signals.memory_fullness = "脑子要溢出了"
        elif signals.token_usage > 0.7 or signals.queue_pressure > 0.6:
            signals.memory_fullness = "有点满"
        elif signals.token_usage > 0.4:
            signals.memory_fullness = "正常"
        else:
            signals.memory_fullness = "清爽"

    # ── 自愈判断 ──────────────────────────────────────────

    def _evaluate_self_heal(self, signals: InteroceptionSignals) -> None:
        # 限流
        if signals.queue_pressure > THROTTLE_QUEUE_PRESSURE:
            signals.throttle = True
            logger.warning("[Interoception] 限流触发: queue_pressure=%.2f", signals.queue_pressure)

        # LLM 退避
        if signals.llm_consecutive_failures >= BACKOFF_CONSECUTIVE_FAILURES:
            signals.backoff_seconds = min(30.0, 2 ** signals.llm_consecutive_failures)
            logger.warning("[Interoception] 退避触发: consecutive_failures=%d, delay=%.1fs",
                           signals.llm_consecutive_failures, signals.backoff_seconds)

        # Provider 切换
        if signals.llm_consecutive_failures >= PROVIDER_SWITCH_CONSECUTIVE_FAILURES:
            signals.provider_switch = True
            logger.warning("[Interoception] Provider 切换触发: consecutive_failures=%d",
                           signals.llm_consecutive_failures)

    # ── SOS 判断 ──────────────────────────────────────────

    def _evaluate_sos(self, signals: InteroceptionSignals) -> None:
        now = time.time()

        # 检查线程死亡
        dead_threads = [name for name, alive in signals.thread_health.items() if not alive]
        if dead_threads:
            reason = f"thread_died_{'_'.join(sorted(dead_threads))}"
            if self._can_send_sos(reason, now):
                names_str = "、".join(dead_threads)
                signals.sos = True
                signals.sos_reason = reason
                signals.sos_message = f"我的心跳停了——{names_str} 线程已退出。"

        # LLM 级联失败
        if signals.llm_consecutive_failures >= SOS_LLM_CASCADE_THRESHOLD:
            reason = "llm_cascade"
            if self._can_send_sos(reason, now):
                signals.sos = True
                signals.sos_reason = reason
                signals.sos_message = "我完全动不了了——LLM 调用连续失败。"

        # 30 分钟追加提醒
        for reason, sent_time in list(self._sos_last_sent.items()):
            if reason not in self._sos_reminder_sent or not self._sos_reminder_sent[reason]:
                if now - sent_time >= SOS_REMINDER_SECONDS:
                    # 检查仍未恢复
                    if not self._is_recovered(reason, signals):
                        signals.sos = True
                        signals.sos_reason = f"{reason}_reminder"
                        signals.sos_message = "还没恢复，可能需要你看一下。"
                        self._sos_reminder_sent[reason] = True

    def _can_send_sos(self, reason: str, now: float) -> bool:
        """检查是否可发送 SOS（冷却检查）。"""
        # 恢复后允许重新触发
        if reason in self._sos_recovered:
            self._sos_recovered.discard(reason)

        last = self._sos_last_sent.get(reason, 0)
        if now - last < SOS_COOLDOWN_SECONDS:
            return False
        self._sos_last_sent[reason] = now
        self._sos_reminder_sent[reason] = False
        return True

    def _is_recovered(self, reason: str, signals: InteroceptionSignals) -> bool:
        """检查异常是否已恢复。"""
        if reason.startswith("thread_died"):
            return all(signals.thread_health.values())
        if reason == "llm_cascade":
            return signals.llm_consecutive_failures < SOS_LLM_CASCADE_THRESHOLD
        return False

    def mark_recovered(self, reason: str) -> None:
        """标记异常已恢复，清空冷却允许再次触发。"""
        self._sos_recovered.add(reason)
        if reason in self._sos_last_sent:
            del self._sos_last_sent[reason]
        if reason in self._sos_reminder_sent:
            del self._sos_reminder_sent[reason]
        logger.info("[Interoception] 异常 %s 已恢复", reason)

    # ── 压力等级 ──────────────────────────────────────────

    def _evaluate_stress(self, signals: InteroceptionSignals) -> None:
        score = 0.0
        score += signals.queue_pressure * 0.2
        score += signals.llm_error_rate * 0.4
        score += (1.0 if not all(signals.thread_health.values()) else 0.0) * 0.4

        if score > 0.7:
            signals.stress_level = "severe"
        elif score > 0.4:
            signals.stress_level = "moderate"
        elif score > 0.15:
            signals.stress_level = "mild"
        else:
            signals.stress_level = "none"
```

- [ ] **Step 2: 提交**

```bash
git add src/xiaomei_brain/consciousness/interoception.py
git commit -m "feat: add Interoception class — centralized body metrics collection and signal evaluation"
```

---

### Task 2: 扩展 SelfBody 增加内感受字段

**Files:**
- Modify: `src/xiaomei_brain/consciousness/self_modules.py` (SelfBody class)

- [ ] **Step 1: 在 SelfBody 增加 7 个内感受字段并更新 to_dict/from_dict**

在 `SelfBody` 类中，在 `attention` 字段后新增：

```python
# 在 `attention: str = "等待用户"` 之后添加：

# ── 新增：内感受字段 ──
thread_health: dict = field(default_factory=lambda: {"layer0": True, "layer2": True})
queue_pressure: float = 0.0
llm_latency_ms: float = 0.0
llm_error_rate: float = 0.0
token_usage: float = 0.0
memory_fullness: str = "清爽"
burning_duration: float = 0.0
```

修改 `to_dict()` 在现有 `d` 之后添加：

```python
d = {
    "energy": self.energy,
    "mood": self.mood or "平静",
    "emotion_intensity": self.emotion_intensity,
    "attention": self.attention,
    "desire_belonging": self.desire_belonging,
    "desire_cognition": self.desire_cognition,
    "desire_achievement": self.desire_achievement,
    "desire_expression": self.desire_expression,
    "dopamine": self.dopamine,
    "serotonin": self.serotonin,
    "cortisol": self.cortisol,
    "oxytocin": self.oxytocin,
    "norepinephrine": self.norepinephrine,
    "motivation_level": self.motivation_level,
    # ── 内感受字段 ──
    "thread_health": self.thread_health,
    "queue_pressure": self.queue_pressure,
    "llm_latency_ms": self.llm_latency_ms,
    "llm_error_rate": self.llm_error_rate,
    "token_usage": self.token_usage,
    "memory_fullness": self.memory_fullness,
    "burning_duration": self.burning_duration,
}
```

修改 `from_dict()` 在 `attention` 恢复后添加：

```python
def from_dict(self, data: dict) -> None:
    if "attention" in data:
        self.attention = data["attention"]
    # ── 内感受字段恢复 ──
    for key in ("thread_health", "queue_pressure", "llm_latency_ms",
                "llm_error_rate", "token_usage", "memory_fullness", "burning_duration"):
        if key in data:
            setattr(self, key, data[key])
```

修改 `get_summary()` 增加身体状态行：

```python
def get_summary(self) -> str:
    base = f"能量{self.energy:.0%}，心情{self.mood}，关注{self.attention}"
    # 内感受简述
    if self.llm_latency_ms > 5000:
        base += "，脑子有点转不动"
    elif self.queue_pressure > 0.5:
        base += "，消息有点多"
    if self.memory_fullness and self.memory_fullness != "清爽":
        base += f"，{self.memory_fullness}"
    return base
```

- [ ] **Step 2: 提交**

```bash
git add src/xiaomei_brain/consciousness/self_modules.py
git commit -m "feat: add 7 interoception fields to SelfBody"
```

---

### Task 3: Layer0 集成 Interoception

**Files:**
- Modify: `src/xiaomei_brain/consciousness/layer0.py`

- [ ] **Step 1: 让 Layer0Autonomous 持有 Interoception 并每 tick 调用**

修改 `__init__` 新增参数：

```python
def __init__(
    self,
    consciousness: Consciousness,
    drive: DriveEngine | None = None,
    tick_interval: float = 1.0,
    debug_file: str = "",
    interoception: Any = None,       # 新增
) -> None:
    self._c = consciousness
    self._drive = drive
    self._tick_interval = tick_interval
    self._running = False
    self._thread: threading.Thread | None = None
    self._lock: threading.RLock = threading.RLock()
    self._debug_file = debug_file
    self._interoception = interoception  # 新增
    ...
```

修改 `_run()` 在 `with self._lock` 内添加 interoception.tick() 调用和 SelfBody 写入：

```python
def _run(self) -> None:
    logger.info("[Layer0] 进入主循环")
    while self._running:
        with self._lock:
            try:
                self._c.tick_L0(agent_state=None)

                # ── 内感受 ──
                if self._interoception:
                    # 采集队列深度（需要 Living 引用，此处通过 consciousness 间接获取）
                    queue_depth = getattr(self._c, '_queue_depth', 0)
                    sig = self._interoception.tick(queue_depth)

                    # 写入 SelfBody
                    body_data = sig.as_body_dict()
                    si = self._c.get_self_image()
                    for key, val in body_data.items():
                        setattr(si.body, key, val)

                    # 读取压缩信号并写入 consciousness（供 ContextAssembler 读取）
                    self._c._interoception_signals = sig

                si = self._c.get_self_image()
                ts = time.strftime("%H:%M:%S")
                self._log(
                    f"{ts} L0 tick | energy={si.body.energy:.2f} "
                    f"mood={si.body.mood} state={si.perception.agent_state} "
                    f"queue={si.body.queue_pressure:.1f} err={si.body.llm_error_rate:.1f}"
                )
            except Exception as e:
                logger.warning("[Layer0] tick_L0 出错: %s", e)
                self._log(f"{time.strftime('%H:%M:%S')} L0 ERROR: {e}")

        time.sleep(self._tick_interval)
```

- [ ] **Step 2: 在 Consciousness 类增加 _queue_depth 和 _interoception_signals 属性**

在 `consciousness/core.py` 的 `Consciousness.__init__` 中添加：

```python
self._queue_depth: int = 0           # 消息队列深度（Living 写入）
self._interoception_signals: Any = None  # InteroceptionSignals（Layer0 写入）
```

- [ ] **Step 3: 在 ConsciousLiving 中创建 Interoception 实例并注入 Layer0**

修改 `consciousness/conscious_living.py` 的 `__init__`，在创建 Layer0Autonomous 之前：

```python
from xiaomei_brain.consciousness.interoception import Interoception

# 创建 Interoception（__init__ 中，Layer0/Layer2 创建之前）
self.interoception = Interoception(burn_start_time=time.time())
```

在 `start()` 方法中，Layer0/Layer2 start() 之后注入线程引用：

```python
# 在 self._layer0.start(); self._layer2.start() 之后
self.interoception.set_threads({
    "layer0": self._layer0._thread,
    "layer2": self._layer2._thread,
})
# 注入 LLM 回调
if hasattr(self.agent, 'llm') and self.agent.llm is not None:
    self.interoception.set_llm_callback(self.agent.llm)
    # 将 interoception 反向注入 LLMClient
    self.agent.llm._interoception = self.interoception
```

修改 `_setup_layers()` 中创建 Layer0Autonomous 时传入 interoception：

```python
self._layer0 = Layer0Autonomous(
    consciousness=self.consciousness,
    drive=self.drive,
    tick_interval=self._config.layer0_interval,
    debug_file=self._get_debug_file("living"),
    interoception=self.interoception,   # 新增
)
```

- [ ] **Step 4: 提交**

```bash
git add src/xiaomei_brain/consciousness/layer0.py src/xiaomei_brain/consciousness/core.py src/xiaomei_brain/consciousness/conscious_living.py
git commit -m "feat: integrate Interoception into Layer0 — tick collects body metrics, writes to SelfBody"
```

---

### Task 4: LLMClient 增加退避和 provider 切换

**Files:**
- Modify: `src/xiaomei_brain/base/llm.py`

- [ ] **Step 1: LLMClient 增加 provider 列表和退避能力**

```python
class LLMClient:
    def __init__(
        self,
        model: str,
        api_key: str,
        base_url: str,
        provider: str | None = None,
        max_retries: int = DEFAULT_MAX_RETRIES,
        timeout: int = DEFAULT_TIMEOUT,
        # ── 新增：备用 provider ──
        fallback_configs: list[dict[str, str]] | None = None,
        interoception: Any = None,  # Interoception 引用
    ) -> None:
        # ... existing init ...

        # ── 新增 ──
        self._fallback_configs = fallback_configs or []
        self._fallback_index = -1  # -1 = 使用主配置
        self._interoception = interoception
        self._last_call_latency_ms: float = 0.0
        self._last_call_error: bool = False

    def _get_active_config(self) -> dict[str, str]:
        """返回当前生效的 base_url/api_key/model。"""
        if self._fallback_index >= 0 and self._fallback_index < len(self._fallback_configs):
            fc = self._fallback_configs[self._fallback_index]
            return {
                "model": fc.get("model", self.model),
                "base_url": fc.get("base_url", self.base_url),
                "api_key": fc.get("api_key", self.api_key),
            }
        return {"model": self.model, "base_url": self.base_url, "api_key": self.api_key}

    def switch_to_fallback(self) -> bool:
        """切换到下一个备用 provider。返回 True 表示切换成功。"""
        self._fallback_index += 1
        if self._fallback_index >= len(self._fallback_configs):
            logger.error("[LLM] 所有备用 provider 已耗尽")
            return False
        cfg = self._fallback_configs[self._fallback_index]
        logger.warning("[LLM] 切换到备用 provider: %s (model=%s)",
                       cfg.get("name", "unknown"), cfg.get("model", "unknown"))
        return True

    def reset_to_primary(self) -> None:
        """切回主 provider（恢复后）。"""
        self._fallback_index = -1
        logger.info("[LLM] 切回主 provider")

    def apply_backoff(self, seconds: float) -> None:
        """执行退避睡眠（供外部在调用前使用）。"""
        if seconds > 0:
            logger.warning("[LLM] 退避 %.1fs", seconds)
            time.sleep(seconds)
```

- [ ] **Step 2: 修改 `_request_with_retry` 和 `chat_stream` 记录每次调用结果**

在 `_request_with_retry` 的 `log_request(True)` 之后和 `chat_stream` 成功后添加：

```python
# 在 _request_with_retry 中
self._last_call_latency_ms = (time.time() - t0) * 1000
self._last_call_error = False
if self._interoception:
    self._interoception.record_llm_call(self._last_call_latency_ms, False)
```

在异常捕获处添加：

```python
# 在 _request_with_retry 的 except/raise 分支中
self._last_call_latency_ms = (time.time() - t0) * 1000
self._last_call_error = True
if self._interoception:
    self._interoception.record_llm_call(self._last_call_latency_ms, True)
```

同样的模式在 `chat_stream` 的 try/except 中添加。

- [ ] **Step 3: 提交**

```bash
git add src/xiaomei_brain/base/llm.py
git commit -m "feat: LLMClient — add fallback provider switching, backoff, and interoception recording"
```

---

### Task 5: Living 基类增加限流和 SOS 信号支持

**Files:**
- Modify: `src/xiaomei_brain/agent/living.py`

- [ ] **Step 1: 添加 sos_signal 属性和限流逻辑到 Living**

在 `Living.__init__` 中添加：

```python
# 内感受信号
self._sos_message: str | None = None       # SOS 待发送消息
self._sos_message_time: float = 0.0         # SOS 消息时间戳
self._interoception_signals: Any = None     # InteroceptionSignals 引用
```

修改 `put_message()` 增加限流逻辑：

```python
def put_message(
    self,
    content: str,
    user_id: str | None = None,
    session_id: str | None = None,
    source: str = "",
    images: list[str] | None = None,
    urgent: bool = False,   # 新增：紧急消息绕过限流
) -> None:
    """放入消息。

    Args:
        urgent: 设为 True 绕过限流（用于 SOS 等紧急消息）。
    """
    if isinstance(content, str):
        content = self._clean_input(content)

    # ── 限流检查 ──
    if not urgent:
        sig = getattr(self, '_interoception_signals', None)
        if sig and getattr(sig, 'throttle', False):
            logger.warning("[Living] 限流激活，丢弃非紧急消息: %.50s", content)
            return

    msg = LivingMessage(
        content=content,
        user_id=user_id or self.user_id,
        session_id=session_id or self.session_id,
        source=source,
        images=images or [],
    )
    self._queue.put_nowait(msg)
```

在 `Living` 基类增加 SOS 消息写入方法：

```python
def set_sos_message(self, message: str) -> None:
    """设置 SOS 消息（由 _heartbeat 调用，基类提供存储）。"""
    self._sos_message = message
    self._sos_message_time = time.time()

def get_and_clear_sos(self) -> str | None:
    """读取并清空 SOS 消息。"""
    msg = self._sos_message
    self._sos_message = None
    return msg

def send_sos_to_channels(self, message: str, channels: list | None = None) -> None:
    """直接通过渠道发送 SOS 消息——绕过 LLM，绕过 anti-spam。

    子类（ConsciousLiving）会覆盖此方法以访问实际的 channel 列表。
    基类提供默认实现：通过 stdout 报告。
    """
    ts = time.strftime("%H:%M:%S")
    print(f"\n[SOS] {ts} {message}", flush=True)
```

- [ ] **Step 2: 提交**

```bash
git add src/xiaomei_brain/agent/living.py
git commit -m "feat: Living — add throttle check in put_message + SOS signal infrastructure"
```

---

### Task 6: ConsciousLiving 集成 SOS 通道

**Files:**
- Modify: `src/xiaomei_brain/consciousness/conscious_living.py`

- [ ] **Step 1: 扩展 _heartbeat() 读取 interoception signals 并处理 SOS**

修改 `_heartbeat()` 方法：

```python
def _heartbeat(self, state: LivingState) -> None:
    if not self._load_consciousness:
        return

    # 同步状态给 Layer 2 线程
    self.consciousness._agent_state = state.value

    # ── 同步队列深度给 consciousness（供 Layer0 的 interoception 读取）──
    self.consciousness._queue_depth = self._queue.qsize()

    # ── 读取 Interoception signals ──
    sig = getattr(self.consciousness, '_interoception_signals', None)
    if sig is not None:
        # 注入给 Living 基类（供限流使用）
        self._interoception_signals = sig

        # SOS 通道
        if getattr(sig, 'sos', False) and getattr(sig, 'sos_message', ''):
            self.send_sos_to_channels(sig.sos_message)

        # 压力事件 → Drive
        if self.drive and sig.stress_level != "none":
            self.drive.on_system_stress(sig.stress_level, "interoception")
        elif self.drive and sig.stress_level == "none":
            self.drive.on_system_healthy()

    # 检查 Layer 2 发出的入梦信号
    if getattr(self.consciousness, '_dream_signal', False):
        self.consciousness._dream_signal = False
        ts = time.strftime("%H:%M:%S")
        self._debug_log("living", f"{ts} Living 收到入梦信号 → HEARTBEAT_DREAM")
        if state == LivingState.SLEEPING:
            self._heartbeat_result = HEARTBEAT_DREAM
```

- [ ] **Step 2: 覆盖 send_sos_to_channels() 实现实际渠道推送**

```python
def send_sos_to_channels(self, message: str, channels: list | None = None) -> None:
    """SOS 紧急推送：绕过 LLM，直接发送到所有可用渠道。"""
    ts = time.strftime("%H:%M:%S")
    sos_text = f"[SOS] {ts}\n{message}"

    adapter = getattr(self, 'channel_adapter', None)
    if adapter:
        try:
            adapter.broadcast(sos_text, channels=channels)
            logger.warning("[Living SOS] 已推送到渠道: %.100s", message)
        except Exception as e:
            logger.error("[Living SOS] 渠道推送失败: %s", e)
    else:
        # 无渠道时走 stdout
        print(f"\n\033[91m{sos_text}\033[0m", flush=True)
```

- [ ] **Step 3: 提交**

```bash
git add src/xiaomei_brain/consciousness/conscious_living.py
git commit -m "feat: ConsciousLiving — SOS channel via _heartbeat, interoception signals relay"
```

---

### Task 7: Drive 增加系统压力事件

**Files:**
- Modify: `src/xiaomei_brain/drive/engine.py`

- [ ] **Step 1: 添加 on_system_stress() 和 on_system_healthy()**

在 `DriveEngine` 类中添加：

```python
def on_system_stress(self, level: str, source: str) -> None:
    """系统压力事件 → 皮质醇上升 + 能量下降。

    Args:
        level: "mild" / "moderate" / "severe"
        source: 压力来源标识
    """
    if level == "mild":
        self.hormone.cortisol = min(1.0, self.hormone.cortisol + 0.05)
        logger.debug("[Drive] 轻度系统压力: %s", source)
    elif level == "moderate":
        self.hormone.cortisol = min(1.0, self.hormone.cortisol + 0.1)
        self.energy.level = max(0.0, self.energy.level - 0.02)
        logger.info("[Drive] 中度系统压力: %s", source)
    elif level == "severe":
        self.hormone.cortisol = min(1.0, self.hormone.cortisol + 0.2)
        self.energy.level = max(0.0, self.energy.level - 0.05)
        logger.warning("[Drive] 严重系统压力!!! %s", source)

def on_system_healthy(self) -> None:
    """系统恢复 → 血清素回升 + 能量恢复。"""
    self.hormone.serotonin = min(1.0, self.hormone.serotonin + 0.02)
    self.energy.level = min(1.0, self.energy.level + 0.01)
```

- [ ] **Step 2: 提交**

```bash
git add src/xiaomei_brain/drive/engine.py
git commit -m "feat: DriveEngine — add on_system_stress() / on_system_healthy() for interoception feedback"
```

---

### Task 8: SelfImage 注入内感受到 system prompt

**Files:**
- Modify: `src/xiaomei_brain/consciousness/self_image_proxy.py`

- [ ] **Step 1: 扩展 _render_body() 注入内感受信息**

修改 `_render_body()` 方法：

```python
def _render_body(self) -> list[str]:
    bo = self.body
    lines: list[str] = ["\n****以下是你当前的身体感受****"]
    _energy = float(getattr(bo, 'energy', 0.5) or 0.5)
    _mood = getattr(bo, 'mood', '平静') or '平静'
    lines.append(f"- 能量：{_energy:.0%}")
    lines.append(f"- 心情：{_mood}")

    # ── 欲望 ──
    desire_parts = []
    for name, attr in [("归属欲", "desire_belonging"), ("认知欲", "desire_cognition"),
                       ("成就欲", "desire_achievement"), ("表达欲", "desire_expression")]:
        val = float(getattr(bo, attr, 0) or 0)
        if val > 0.5:
            desire_parts.append(f"{name}偏高（{val:.0%}）")
    if desire_parts:
        lines.append(f"- 欲望：{'，'.join(desire_parts)}")

    # ── 激素 ──
    _dopa = float(getattr(bo, 'dopamine', 0.5) or 0.5)
    _sero = float(getattr(bo, 'serotonin', 0.5) or 0.5)
    _cort = float(getattr(bo, 'cortisol', 0) or 0)
    lines.append(f"- 多巴胺{_dopa:.0%}，血清素{_sero:.0%}，皮质醇{_cort:.0%}")

    # ── 内感受：身体状态（人格化表达）──
    _queue_p = float(getattr(bo, 'queue_pressure', 0) or 0)
    _latency = float(getattr(bo, 'llm_latency_ms', 0) or 0)
    _err_rate = float(getattr(bo, 'llm_error_rate', 0) or 0)
    _token = float(getattr(bo, 'token_usage', 0) or 0)
    _burn = float(getattr(bo, 'burning_duration', 0) or 0)
    _mem = getattr(bo, 'memory_fullness', '') or ''

    body_desc = self._render_interoception(_queue_p, _latency, _err_rate, _token, _burn, _mem)
    if body_desc:
        lines.append(f"- 状态：{body_desc}")

    return lines

@staticmethod
def _render_interoception(
    queue_p: float, latency_ms: float, error_rate: float,
    token_usage: float, burning_hours: float, memory_fullness: str,
) -> str:
    """将内感受数据转为人格化的身体状态描述。"""
    parts = []

    # burning duration
    if burning_hours < 0.5:
        parts.append("刚醒，精神不错")
    elif burning_hours < 3:
        parts.append(f"已经活跃了{burning_hours:.0f}小时，状态良好")
    elif burning_hours < 8:
        parts.append(f"连续运转了{burning_hours:.0f}小时，有点乏了")
    else:
        parts.append(f"燃烧了{burning_hours:.0f}小时，需要休息了")

    # queue pressure
    if queue_p > 0.7:
        parts.append("消息堆积很多，应接不暇")
    elif queue_p > 0.4:
        parts.append("消息有点多")

    # latency
    if latency_ms > 10000:
        parts.append("脑子转得很慢")
    elif latency_ms > 5000:
        parts.append("脑子有点转不动")

    # error rate
    if error_rate > 0.5:
        parts.append("最近老出错，状态很差")
    elif error_rate > 0.2:
        parts.append("出了几次错，不太舒服")

    # token usage
    if token_usage > 0.85:
        parts.append("脑子快满了，很快就要忘记前面的事")
    elif token_usage > 0.6:
        parts.append("说了不少了，记忆在缩减")

    # memory
    if memory_fullness and memory_fullness != "清爽":
        parts.append(memory_fullness)

    return "；".join(parts) if parts else "一切正常"
```

- [ ] **Step 2: 提交**

```bash
git add src/xiaomei_brain/consciousness/self_image_proxy.py
git commit -m "feat: SelfImage._render_body() — inject interoception data as personified body description"
```

---

### Task 9: P0 验证——冒烟测试

**Files:**
- Create: `examples/test_interoception.py`

- [ ] **Step 1: 写冒烟测试**

```python
"""P0 冒烟测试：验证 Interoception 的基本功能。"""
import sys
import time
import threading

# Verify imports work
from xiaomei_brain.consciousness.interoception import (
    Interoception, InteroceptionSignals,
    THROTTLE_QUEUE_PRESSURE, BACKOFF_CONSECUTIVE_FAILURES,
    SOS_LLM_CASCADE_THRESHOLD, SOS_COOLDOWN_SECONDS,
)

def test_basic_creation():
    """Interoception 能正常创建并 tick。"""
    io = Interoception()
    sig = io.tick(queue_depth=0)
    assert sig is not None
    assert sig.queue_pressure == 0.0
    assert sig.stress_level == "none"
    assert not sig.throttle
    assert not sig.sos
    print("PASS: basic creation")

def test_thread_health():
    """检测线程存活状态。"""
    alive_thread = threading.Thread(target=time.sleep, args=(10,), daemon=True)
    alive_thread.start()

    io = Interoception()
    io.set_threads({"test_thread": alive_thread})
    sig = io.tick(queue_depth=0)
    assert sig.thread_health["test_thread"] is True
    print("PASS: thread health (alive detected)")

def test_queue_pressure():
    """队列压力正确计算。"""
    io = Interoception(max_queue_size=100)
    sig = io.tick(queue_depth=85)
    assert sig.queue_pressure == 0.85
    assert sig.throttle  # > 0.8
    print("PASS: queue pressure + throttle trigger")

def test_llm_backoff():
    """LLM 连续失败触发退避。"""
    io = Interoception()
    for _ in range(BACKOFF_CONSECUTIVE_FAILURES):
        io.record_llm_call(1000, is_error=True)
    sig = io.tick(queue_depth=0)
    assert sig.backoff_seconds > 0
    assert sig.llm_consecutive_failures >= BACKOFF_CONSECUTIVE_FAILURES
    print("PASS: LLM backoff trigger")

def test_llm_provider_switch():
    """LLM 连续失败触发 provider 切换。"""
    io = Interoception()
    for _ in range(5):
        io.record_llm_call(1000, is_error=True)
    sig = io.tick(queue_depth=0)
    assert sig.provider_switch
    print("PASS: LLM provider switch trigger")

def test_sos_trigger():
    """LLM 级联失败触发 SOS。"""
    io = Interoception()
    io.set_threads({"layer0": threading.current_thread(), "layer2": threading.current_thread()})
    for _ in range(SOS_LLM_CASCADE_THRESHOLD):
        io.record_llm_call(1000, is_error=True)
    sig = io.tick(queue_depth=0)
    assert sig.sos
    assert "LLM" in sig.sos_message
    print("PASS: SOS trigger on cascade failure")

def test_sos_cooldown():
    """SOS 5 分钟内不重复触发。"""
    io = Interoception()
    io.set_threads({"layer0": threading.current_thread(), "layer2": threading.current_thread()})
    for _ in range(SOS_LLM_CASCADE_THRESHOLD):
        io.record_llm_call(1000, is_error=True)

    sig1 = io.tick(queue_depth=0)
    assert sig1.sos

    sig2 = io.tick(queue_depth=0)
    assert not sig2.sos  # 冷却中
    print("PASS: SOS cooldown (no repeat within 5 min)")

def test_stress_level():
    """压力等级正确计算。"""
    io = Interoception()
    # 无压力
    sig = io.tick(queue_depth=0)
    assert sig.stress_level == "none"

    # 中等压力（高错误率）
    for _ in range(5):
        io.record_llm_call(1000, is_error=True)
    sig = io.tick(queue_depth=0)
    assert sig.stress_level in ("moderate", "severe")
    print("PASS: stress level calculation")

def test_signals_as_body_dict():
    """as_body_dict 返回完整数据。"""
    io = Interoception()
    sig = io.tick(queue_depth=10)
    d = sig.as_body_dict()
    assert "thread_health" in d
    assert "queue_pressure" in d
    assert "llm_latency_ms" in d
    assert "llm_error_rate" in d
    assert "token_usage" in d
    assert "memory_fullness" in d
    assert "burning_duration" in d
    print("PASS: as_body_dict returns all 7 fields")

def test_sos_recovery():
    """异常恢复后能重新触发 SOS。"""
    io = Interoception()
    io.set_threads({"layer0": threading.current_thread(), "layer2": threading.current_thread()})
    for _ in range(SOS_LLM_CASCADE_THRESHOLD):
        io.record_llm_call(1000, is_error=True)

    sig = io.tick(queue_depth=0)
    assert sig.sos

    # 标记恢复
    io.mark_recovered("llm_cascade")

    # 再次级联失败
    for _ in range(SOS_LLM_CASCADE_THRESHOLD):
        io.record_llm_call(1000, is_error=True)
    sig = io.tick(queue_depth=0)
    assert sig.sos  # 恢复后能再次触发
    print("PASS: SOS recovery + re-trigger")

def run_all():
    tests = [
        test_basic_creation,
        test_thread_health,
        test_queue_pressure,
        test_llm_backoff,
        test_llm_provider_switch,
        test_sos_trigger,
        test_sos_cooldown,
        test_stress_level,
        test_signals_as_body_dict,
        test_sos_recovery,
    ]
    failed = 0
    for t in tests:
        try:
            t()
        except Exception as e:
            print(f"FAIL: {t.__name__}: {e}")
            import traceback
            traceback.print_exc()
            failed += 1
    print(f"\n{failed}/{len(tests)} tests failed")
    return failed

if __name__ == "__main__":
    sys.exit(run_all())
```

- [ ] **Step 2: 运行测试**

```bash
source /home/iamlibai/workspace/python_env_common/bin/activate
PYTHONPATH=src python3 examples/test_interoception.py
```

Expected: All 10 tests pass, 0 failures.

- [ ] **Step 3: 提交**

```bash
git add examples/test_interoception.py
git commit -m "test: add interoception smoke tests (10 cases)"
```

---

### Task 10: P1 集成测试——完整链路验证

**Files:**
- Create: `examples/test_interoception_integration.py`

- [ ] **Step 1: 端到端集成测试**

```python
"""P1 集成测试：验证 Interoception 完整链路——采集→SelfBody→Drive→SOS。

启动简化的 ConsciousLiving，验证：
1. Interoception 正确注入 Layer0
2. SelfBody 内感受字段实时更新
3. LLM 错误触发退避
4. Drive 接收压力事件
"""
import sys
import time
import threading

def test_integration_full_chain():
    """完整链路：Interoception → SelfBody → Drive → 压力事件。"""
    from xiaomei_brain.consciousness.interoception import Interoception
    from xiaomei_brain.drive.engine import DriveEngine

    # 1. 创建 Interoception
    io = Interoception()
    io.set_threads({"layer0": threading.current_thread()})

    # 2. 创建 DriveEngine（不加载文件）
    drive = DriveEngine(agent_id="test_interoception", load=False)

    # 3. 模拟 LLM 错误
    for _ in range(3):
        io.record_llm_call(5000, is_error=True)

    # 4. Tick
    sig = io.tick(queue_depth=50)

    # 5. 验证 SelfBody 数据
    body_data = sig.as_body_dict()
    assert body_data["llm_error_rate"] == 1.0
    assert body_data["llm_consecutive_failures"] == 3  # 注意这里访问的是 signals 级别
    print("PASS: body data correct after LLM errors")

    # 6. 验证 Drive 压力事件
    cortisol_before = drive.hormone.cortisol
    drive.on_system_stress(sig.stress_level, "test")
    if sig.stress_level != "none":
        assert drive.hormone.cortisol > cortisol_before
        print("PASS: Drive cortisol increased by stress")
    else:
        print("SKIP: no stress detected (expected for non-severe condition)")

    # 7. 验证恢复
    io.record_llm_call(500, is_error=False)
    io.record_llm_call(600, is_error=False)
    sig2 = io.tick(queue_depth=0)
    drive.on_system_healthy()
    assert drive.hormone.serotonin > 0.5  # base was 0.5
    print("PASS: Drive serotonin increased on recovery")

    print("\n=== Integration test PASSED ===")

if __name__ == "__main__":
    test_integration_full_chain()
```

- [ ] **Step 2: 运行集成测试**

```bash
source /home/iamlibai/workspace/python_env_common/bin/activate
PYTHONPATH=src python3 examples/test_interoception_integration.py
```

Expected: All assertions pass.

- [ ] **Step 3: 提交**

```bash
git add examples/test_interoception_integration.py
git commit -m "test: add interoception integration test — full chain Interoception→Drive"
```

---

## 实现顺序

| 顺序 | 任务 | 依赖 |
|------|------|------|
| 1 | Interoception 类 | 无 |
| 2 | SelfBody 扩展 | 无（字段定义先行） |
| 3 | Layer0 集成 | 1 |
| 4 | LLMClient 退避 | 1 |
| 5 | Living 限流+SOS | 1 |
| 6 | ConsciousLiving SOS通道 | 5 |
| 7 | Drive 压力事件 | 无 |
| 8 | SelfImage 注入 | 2 |
| 9 | P0 冒烟测试 | 1 |
| 10 | P1 集成测试 | 3, 7 |
