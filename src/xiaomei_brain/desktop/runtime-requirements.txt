# Core dependencies bundled into the Desktop Agent Runtime.
# Keep this archive small: it only contains the Agent and the libraries needed
# before first-run setup. Local inference libraries are installed afterwards
# as one shared CPU or CUDA component and model weights remain outside the app.
requests>=2.31.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pyyaml>=6.0
lancedb>=0.4.0
rich>=13.0.0
pydantic>=2.0
numpy>=1.24.0
jsonschema>=4.0
psutil>=5.9.0
mcp>=1.0.0
lark-oapi
dingtalk-stream

Pillow>=10.0
