name: feishu
version: "1.0.0"
description: 飞书/Lark 平台对接
kind: channel
channel: feishu
configSchema:
  type: object
  additionalProperties: false
  properties:
    appId:
      type: string
    appSecret:
      type: string
    accountId:
      type: string
      description: 当前 Agent 绑定的飞书账号配置标识
    displayName:
      type: string
      description: Desktop 中展示的机器人名称
    groupAllowFrom:
      type: array
      description: 允许的群聊列表（* 表示全部）
      items:
        type: string
    groupPolicy:
      type: string
      description: 群聊策略（open/allowlist/disabled）
      enum: [open, allowlist, disabled]
